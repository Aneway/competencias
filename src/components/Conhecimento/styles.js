import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        width: '100%',
        backgroundColor: '#eef0f2',
        borderRadius: 5,
        padding: 16,
        marginTop: 16
    },
    descricao:{
        fontSize: 16,
        fontWeight: '500',
        color: '#0b090a'
    }
})